# BibTeX Clean 领域词汇表

本文件记录 BibTeX Clean 插件的领域术语。只包含业务概念，不包含实现细节。

## 核心术语

### 清理（Cleaning）

对 Zotero 条目的字段应用规则，修改字段值，使其符合 BibTeX 输出或元数据规范的过程。

> 用户原称"清洗"，统一为"清理"。代码中以 `clean*` 前缀表示。

### 目标条目（Target Item）

用户选中的、将要被清理的 Zotero 条目。一次清理操作可以作用于一个或多个目标条目。

### 清理规则（Cleaning Rule）

针对特定字段的转换规则。当前规则：

- `author` 规则：将字段值中的 `;` 替换为 `and`。
- `issue` 规则：移除 `No.` 前缀与汉字 `第`、`期`。
- `volume` 规则：移除 `Vol.` 前缀。

### 变更（Change）

对单个字段应用一条清理规则后，该字段从旧值到新值的转换。一个目标条目可以产生零个或多个变更。

变更类型按职责拆分为两个更窄的类型：

- **字段变更（FieldChange）**：`{ itemKey, field, oldValue, newValue }`，writer 消费的纯数据载荷。
- **展示变更（DisplayChange）**：`FieldChange & { itemTitle }`，对话框和通知消费的展示数据。

`computeChanges` 返回 `DisplayChange`；writer 边界（`applyChanges` / `undoChanges`）接收 `FieldChange`。

### 模拟运行（Dry Run）

在请求用户确认之前，先计算所有目标条目会产生的变更，但不写入 Zotero 数据库的过程。

### 确认对话框（Confirmation Dialog）

展示模拟运行结果，提示用户清理操作会导致什么变化，并请求用户确认或取消的界面。

### 通知（Notification）

清理操作完成后向用户展示的反馈信息。包括：清理成功（带撤销链接）、清理部分成功、清理失败详情、撤销成功、无需清理。

筛选删除的通知另有一套文案：删除成功（已删除 N 项，含 M 条笔记，已移入回收站）、删除部分成功、删除失败详情。删除通知不带撤销链接——恢复入口是 Zotero 回收站。

### 清理会话（Clean Session）

一次完整的清理操作流程：选择目标条目 → 模拟运行 → 用户确认 → 写入变更 → 通知结果。插件在内存中维护一个清理会话存储，记录最近一次成功清理的变更，用于支持撤销。

### 清理会话存储（Clean Session Store）

`CleanSessionStore` 管理最近一次清理操作的撤销状态。提供 `record`（记录并深克隆变更）、`current`（查看）、`consume`（取出并清空）、`hasUndo`（是否可撤销）四个方法。

### 撤销（Undo）

恢复最近一次清理操作对目标条目字段的修改。清理会话存储在撤销后立即清除记录，防止重复撤销。

### 子条目（Child Item）

以某个条目为父的附件或笔记。Zotero 中附件与笔记都可以不依附任何条目而独立存在，那种情况不称子条目。

### 链接附件（Link Attachment）

指向外部网页的附件，Zotero 中对应两种类型：**网页快照**（把网页副本存进库内）与**网页链接**（只保存外部网址）。这两种归入同一个概念，因为它们处理后果相同；指向本地文件的附件（导入的文件、链接到文件的附件）不是链接附件。

### 笔记（Note）

Zotero 的笔记条目。作为某条目的子条目存在时是子笔记，不依附任何条目时是独立笔记。

### 候选项（Candidate）

一次筛选删除中可能被删除的子条目集合：选中条目的直接子附件与直接子笔记。父条目本身永远不是候选项。

### 删除（Delete）

本插件所说的删除指移入 Zotero 回收站，条目仍然存在、可由用户恢复。从库中永久移除（彻底删除）不在此列。

### 筛选删除（Filter Delete）

按条件筛选候选项、由用户勾选后把选中的子条目移入回收站的流程。与清理的区别：清理规范化字段值并产生变更、可撤销；筛选删除移除子条目、不产生变更、无插件内撤销。

### 重复附件（Duplicate Attachment）

同一父条目下，与另一附件判定相同的附件：文件附件（导入文件、链接文件）按文件名相同（忽略大小写）判定，链接附件（网页快照、网页链接）按 URL 精确相同判定。典型来源是合并重复条目：两个条目各自的附件被并到同一个父条目下。不同父条目下的同名附件不算重复。

### 重复组（Duplicate Group）

分组键相同且成员不少于 2 的一组重复附件。分组键带类型前缀，文件附件与链接附件的键不会互相撞车；缺文件名或缺 URL 的附件不参与分组。

### 保留者（Keeper）

重复组里默认不被删除的那一份：批注最多的优先（删除带批注的附件会连带丢失批注），批注数并列时取添加最早的。对话框中带"保留"徽章，用户可手动改勾选换一份保留。
